// Vitest test environment setup.
// Loaded by vitest.config.ts via `setupFiles`. Imports the
// jest-dom matcher extensions so tests can use `toBeInTheDocument`,
// `toHaveTextContent`, etc.

import '@testing-library/jest-dom/vitest';
