"""CLI package for Agent Baton.

This package provides the ``baton`` command-line interface built on
argparse. The entry point is agent_baton.cli.main.main, which
auto-discovers command modules and dispatches to the matching handler.
"""
