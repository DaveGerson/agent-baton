[project]
name = "medium-project-fixture"
version = "0.1.0"
description = "Fixture repo for tests/e2e/test_manager_mode_planning.py (manager-mode PMO planning E2E, PRD Milestone 8)."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests_fixture"]
